jaffle_shop:
  outputs:
    default:
      account: %ACCOUNT
      client_session_keep_alive: false
      database: %DATABASE
      password: %PASSWORD
      role: %ROLE
      schema: %SCHEMA
      threads: 4
      type: snowflake
      user: %USER
      warehouse: %WAREHOUSE
  target: default
